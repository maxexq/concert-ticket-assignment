import { MainLayout } from "@/components/layout";
import { TestToastButton } from "@/components";

const Home = () => {
  return (
    <MainLayout>
      <TestToastButton />
    </MainLayout>
  );
};

export default Home;
